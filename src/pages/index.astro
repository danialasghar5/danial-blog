---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Hero from '../components/Hero.astro';
import Metrics from '../components/Metrics.astro';
import PostCard from '../components/PostCard.astro';
import Projects from '../components/Projects.astro';
import Newsletter from '../components/Newsletter.astro';
import TopicChip from '../components/TopicChip.astro';
import { getCollection } from 'astro:content';
import { SITE_TITLE, SITE_DESCRIPTION, ROLE } from '../consts';
import { liveTopics } from '../data/topics';

const posts = (await getCollection('blog'))
  .filter((p) => !p.data.draft)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 3);

const hubs = liveTopics();
---
<html lang="en">
  <head><BaseHead title={`${SITE_TITLE} — ${ROLE}`} description={SITE_DESCRIPTION} /></head>
  <body>
    <Header />
    <main id="main-content">
      <Hero />
      <Metrics />

      {posts.length > 0 && (
        <section class="container block">
          <div class="section-head">
            <h2>Latest writing</h2>
            <a class="more" href="/blog">All posts &rarr;</a>
          </div>
          <div class="grid-cards">
            {posts.map(post => <PostCard post={post} />)}
          </div>
        </section>
      )}

      {hubs.length > 0 && (
        <section class="container block">
          <div class="section-head">
            <h2>Browse by topic</h2>
            <a class="more" href="/topics">All topics &rarr;</a>
          </div>
          <div class="chip-row">
            {hubs.map((t) => <TopicChip slug={t.slug} />)}
          </div>
        </section>
      )}

      <Projects />
      <Newsletter />
    </main>
    <Footer />
  </body>
</html>
<style>
  .block { padding-block: var(--s-6); }
  .chip-row { display: flex; flex-wrap: wrap; gap: .5rem; }
</style>
